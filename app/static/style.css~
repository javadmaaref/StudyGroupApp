/* app/static/style.css */
/* Reset and base styles */
* { margin: 0; padding: 0; box-sizing: border-box; }
body { font-family: 'Inter', sans-serif; background-color: #f8f9fa; color: #333; line-height: 1.6; }
::-webkit-scrollbar { display: none; }
body { -ms-overflow-style: none; scrollbar-width: none; }

/* Utility classes */
.shadow-sm { box-shadow: 0 0.125rem 0.25rem rgba(0,0,0,0.075) !important; }
.hover-lift { transition: transform 0.2s ease-in-out; }
.hover-lift:hover { transform: translateY(-5px); }

/* Typography */
.display-4 { font-weight: 700; color: #2d3748; }
.lead { color: #4a5568; }
.text-muted { color: #6c757d !important; }

/* Layout */
.container { padding: 0 1.25rem; }
.mb-4 { margin-bottom: 1.5rem !important; }
.gap-3 { gap: 1rem; }
.px-4 { padding-left: 1.5rem !important; padding-right: 1.5rem !important; }
.py-3 { padding-top: 1rem !important; padding-bottom: 1rem !important; }

/* Navbar */
.navbar { padding: 0.8rem 1rem; margin-bottom: 2rem; box-shadow: 0 2px 4px rgba(0,0,0,.1); }
.navbar-brand { font-weight: 600; padding-right: 2rem; }
.nav-link { padding: 0.5rem 1rem !important; position: relative; }
.nav-link:hover, .nav-link.active { color: rgba(255, 255, 255, 0.95) !important; }

/* Cards */
.card { border: none; box-shadow: 0 2px 4px rgba(0,0,0,.05); transition: transform 0.2s ease-in-out; }
.card:hover { transform: translateY(-5px); box-shadow: 0 4px 8px rgba(0,0,0,.1); }
.card-header { background-color: #fff; border-bottom: 1px solid rgba(0,0,0,.125); }

/* Buttons */
.btn { transition: all 0.2s ease-in-out; }
.btn-primary { background-color: #007bff; border-color: #007bff; }
.btn-primary:hover { background-color: #0056b3; border-color: #0056b3; }
.btn-danger { background-color: #dc3545; border-color: #dc3545; }
.btn-danger:hover { background-color: #c82333; border-color: #bd2130; }
.btn-sm { padding: 0.25rem 0.5rem; font-size: 0.875rem; border-radius: 0.2rem; }
.btn:focus { box-shadow: none !important; }

/* Forms */
.form-control:focus { border-color: #80bdff; box-shadow: 0 0 0 0.2rem rgba(0,123,255,.25); }
input.form-control, textarea.form-control { border-radius: 0.375rem; border: 1px solid #dee2e6; padding: 0.75rem 1rem; }

/* List groups */
.list-group-item { border: 1px solid rgba(0,0,0,.125); margin-bottom: 0.5rem; border-radius: 0.25rem !important; }

/* Badges */
.badge { font-weight: 500; padding: 0.5em 1em; }

/* Alerts */
.alert { border-radius: 0.25rem; border: none; box-shadow: 0 2px 4px rgba(0,0,0,.05); }

/* Home page */
.bg-gradient-primary { background: linear-gradient(145deg, #f8f9fa 0%, #e9ecef 100%); }
.min-vh-75 { min-height: 75vh; }
.gradient-circle {
    position: absolute; width: 300px; height: 300px;
    background: linear-gradient(145deg, rgba(0,123,255,0.1) 0%, rgba(0,123,255,0.05) 100%);
    border-radius: 50%; top: 50%; left: 50%; transform: translate(-50%, -50%); z-index: 0;
}
.emoji-container { position: relative; z-index: 1; font-size: 180px; padding: 20px; }

/* Feature icons and steps */
.feature-icon { width: 48px; height: 48px; display: flex; align-items: center; justify-content: center; }
.step-circle {
    width: 60px; height: 60px; background: #007bff; color: white; border-radius: 50%;
    display: flex; align-items: center; justify-content: center; font-size: 24px; font-weight: bold; margin: 0 auto;
}
.step-circle-small {
    width: 32px; height: 32px; min-width: 32px; background: #007bff; color: white; border-radius: 50%;
    display: flex; align-items: center; justify-content: center; font-weight: bold;
}

/* Chat styles */
.chat-card { height: calc(100vh - 140px); display: flex; flex-direction: column; }
.chat-messages { flex: 1; overflow-y: auto; padding: 1rem; display: flex; flex-direction: column-reverse; }
.chat-message { margin-bottom: 1rem; max-width: 80%; }
.chat-message-own { margin-left: auto; }
.chat-message-content {
    background-color: #f8f9fa; padding: 0.75rem; border-radius: 1rem; position: relative;
}
.chat-message-own .chat-message-content { background-color: #007bff; color: white; }
.chat-message-own .chat-message-content .text-muted { color: rgba(255, 255, 255, 0.8) !important; }
.chat-message-header {
    display: flex; justify-content: space-between; align-items: center;
    margin-bottom: 0.25rem; font-size: 0.875rem;
}
.chat-message-text { word-break: break-word; }
.card-footer { background-color: white; border-top: 1px solid rgba(0,0,0,.125); padding: 1rem; }

/* Profile page */
.activity-item { border-left: 3px solid #007bff; margin-bottom: 1rem; padding-left: 1rem; }
.text-truncate { max-width: 200px; }

/* Responsive styles */
@media (max-width: 991.98px) {
    .navbar-nav { padding: 0.5rem 0; }
    .navbar .text-light { padding: 0.5rem 1rem; }
    .emoji-container { font-size: 140px; }
    .gradient-circle { width: 240px; height: 240px; }
    .display-4 { font-size: 2.5rem; }
    .min-vh-75 { min-height: auto; }
}

@media (max-width: 768px) {
    .display-4 { font-size: 2rem; }
    .py-5 { padding-top: 2rem !important; padding-bottom: 2rem !important; }
    .card { margin-bottom: 1rem; }
    .chat-card { height: calc(100vh - 200px); }
    .chat-message { max-width: 90%; }
}

/* RSVP component styles */
.rsvp-buttons {
  display: flex;
  gap: 0.5rem;
}

.rsvp-button {
  transition: all 0.2s ease-in-out;
}

.rsvp-button.active {
  transform: scale(1.05);
}

.rsvp-list {
  max-height: 200px;
  overflow-y: auto;
  margin-top: 1rem;
}

.rsvp-item {
  padding: 0.5rem;
  border-bottom: 1px solid #eee;
  margin-bottom: 0.5rem;
  animation: fadeIn 0.3s ease-in;
}

.rsvp-status {
  padding: 0.2rem 0.5rem;
  border-radius: 0.25rem;
  margin-left: 0.5rem;
  font-size: 0.875rem;
}

.rsvp-status.going {
  background-color: #d4edda;
  color: #155724;
}

.rsvp-status.not_going {
  background-color: #f8d7da;
  color: #721c24;
}

.rsvp-status.maybe {
  background-color: #fff3cd;
  color: #856404;
}

.rsvp-comment {
  margin: 0.25rem 0;
  font-size: 0.875rem;
  color: #666;
}

@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(-10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.alert {
  box-shadow: 0 2px 5px rgba(0,0,0,0.2);
  animation: slideIn 0.3s ease-out;
}

@keyframes slideIn {
  from {
    transform: translateX(100%);
  }
  to {
    transform: translateX(0);
  }
}

html { scroll-behavior: smooth; }
